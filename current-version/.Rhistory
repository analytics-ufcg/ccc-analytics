require(fastcluster)
df = read.csv("~/git/ccc/ccc-analytics-2.0/data/dissimilaridade.csv", header = FALSE, sep=",")
library(fpc)
library(plyr)
library(RODBC)
channel <- odbcConnect("VerticaDSN")
setwd("~/ccc/ccc-analytics-2.0")
setwd("~/current-version")
setwd("~/ccc/current-version")
Mode <- function(x) {
ux <- unique(x)
ux[which.max(tabulate(match(x, ux)))]
ano_de_entrada <- function (mat) {
as.integer(substr(as.character(mat), 2, 3))
}
#CSV gerado em python com a dissimilaridade entre as disciplinas
dataframe = read.csv("data/dissimilaridade_distancia.csv", header = FALSE, sep=",")
}
Mode <- function(x) {
ux <- unique(x)
ux[which.max(tabulate(match(x, ux)))]
}
#########Função que retorna o periodo de entrada de uma matrícula passada. Ex de matrícula: 107111111. Output: 071
ano_de_entrada <- function (mat) {
as.integer(substr(as.character(mat), 2, 3))
}
##################################leitura de arquivos
#CSV gerado em python com a dissimilaridade entre as disciplinas
dataframe = read.csv("data/dissimilaridade_distancia.csv", header = FALSE, sep=",")
#Dataframe com períodos relativos que os alunos pagaram as disciplinas
aluno_disciplina_periodo = read.csv("data/arquivo_notas_disciplinas_periodo.csv", header = T, sep=",")
